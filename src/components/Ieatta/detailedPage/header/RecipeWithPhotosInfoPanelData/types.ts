import type {IFBRecipe} from '@src/types/firebase';

type RecipeWithPhotosInfoPanelDataProps = {
    recipe: IFBRecipe;
};

export type {
    // eslint-disable-next-line import/prefer-default-export
    RecipeWithPhotosInfoPanelDataProps,
};
